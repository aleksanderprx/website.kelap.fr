/* ============================================
   KELAP - Portfolio Interactions
   ============================================ */

const Portfolio = (() => {

    function init() {
        // Cursor hover is handled by cursor.js
    }

    return { init };
})();
